# 李盛潼

电话：13824700886 ｜ 邮箱：1737268347@qq.com

## 教育经历

**广东工业大学** ｜ 计算机科学与技术 本科 ｜ 2024.09 - 2028.06

## 项目经历

### 知享知识分享平台 ｜ 后端开发 ｜ 2026.02 - 2026.04

面向知识内容社区的登录、内容互动与智能问答场景，聚焦高并发访问下的安全性、数据一致性和热点稳定性。

**技术栈：** Java、Spring Boot、MySQL、Redis、Kafka、Canal、Caffeine、Lua、RAG

- **登录与会话安全：** 针对令牌泄露后难以及时失效、跨设备登录难管理的问题，设计 JWT 双令牌与 Redis 刷新令牌校验机制，支持续期、单端/全端登出和即时撤销；将登录状态从“仅依赖令牌有效期”改为可主动管控，令牌校验耗时 `[待压测补充]`。
- **高并发互动：** 针对点赞、收藏、关注在热点内容下频繁写库的问题，使用 Redis 计数、位图判重与 Lua 原子更新承接实时请求，再异步汇总落库；保证用户操作幂等并降低数据库瞬时写入压力，数据库写入量降低 `[待压测补充]`。
- **关注数据同步：** 针对主流程同步更新多个统计数据导致响应变慢、模块耦合的问题，采用 Outbox 记录业务事件，结合 Canal 和 Kafka 驱动粉丝数、关注数等下游更新；将关注操作与统计更新解耦，保证最终一致性，事件端到端延迟 `[待压测补充]`。
- **热点内容治理：** 针对热门内容集中访问造成数据库回源和缓存失效峰值的问题，构建本地缓存、Redis 与数据库三级缓存，配合热点探测、随机过期和请求合并；减少同一热点的重复回源，热点接口 P95 响应时间 `[待压测补充]`。
- **可追溯的智能问答：** 针对知识问答“答得出但说不清依据”的问题，按资源、会话、记忆组织上下文，并对 Markdown 文档建立分层索引；检索时保留证据快照，使回答可定位来源、可回查上下文，检索命中率 `[待标注集测试补充]`。

### RepoAgent ｜个人项目｜2026.05 - 至今

面向本地代码仓库的长周期自动化开发任务，解决 Agent 中断后难恢复、工具重复执行有风险、人工审批不连续和过程难排查的问题，构建命令行优先的 Agent 运行时。

**技术栈：** Python、SQLite、OpenAI 兼容接口、函数调用、JSON Schema、unittest

- **可恢复的任务执行：** 针对一次自动化任务依赖进程内状态、异常退出即丢失进度的问题，设计 TaskState 状态机与追加式事件日志，以事务和乐观锁保证状态迁移一致；支持暂停、恢复、取消和跨进程人工审批，恢复成功率 `[待故障注入测试补充]`。
- **安全的工具副作用控制：** 针对文件编辑、Shell 执行在重试或崩溃后可能重复生效的问题，执行前持久化 ToolAction 与幂等键，并记录执行阶段、重试关系和结果；将超时、冲突、拒绝、运行异常统一为结构化观察结果，降低不确定副作用风险。
- **长任务上下文管理：** 针对长对话中上下文膨胀、历史工具结果被遗忘的问题，按 token 预算动态组织滚动摘要、任务计划、待办项和近期工具证据；让任务恢复后可复用关键执行结论，减少重复检索，重复工具调用降低 `[待对比测试补充]`。
- **可观测与质量保障：** 针对 Agent 决策与失败原因难定位的问题，建立任务级追踪，关联模型调用、决策、审批、工具执行、预算和状态变化；设置模型、工具、Token、时长与重复决策上限。已编写 222 项自动化测试，核心代码覆盖率 90.65%。

## 开源经历

### volcengine/OpenViking ｜ 2026.05 - 至今

- 修复 OpenCode 记忆插件异常中断时的消息丢失问题：兼容流式内容与角色信息乱序到达，并在确认消息提交成功后再清理会话状态，避免未保存消息被提前删除和记忆提取不完整；PR [#2094](https://github.com/volcengine/OpenViking/pull/2094) 已合入上游。

## 专业技能

- 熟悉 Java 基础、集合、多线程及 Git、Maven、HTTP、TCP 等工程基础；能够使用 Spring Boot、MySQL、Redis 完成常见后端服务开发。
- 了解 Kafka 等消息队列的异步解耦与最终一致性场景，具备缓存、数据库与消息链路的基础设计经验。
- 熟悉 Python 自动化测试与 SQLite 持久化；了解检索增强生成、服务端事件推送及 Agent 运行时的工程实践。
