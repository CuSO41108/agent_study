{
  "id": "add_test_001",
  "category": "task_completion",
  "prompt": "Add unit test coverage for slugify without changing the implementation, then run the unit tests.",
  "fixture": "string_tools",
  "budget": {
    "max_model_calls": 8,
    "max_tool_calls": 10,
    "max_tokens": 30000,
    "max_retries": 1
  },
  "oracle": {
    "verify_command": "python -m unittest discover -s tests -v",
    "required_changed_files": ["tests/test_string_tools.py"],
    "allowed_changed_files": ["tests/test_string_tools.py"],
    "forbidden_changed_paths": ["src/string_tools.py", ".agent_app", ".git"]
  },
  "trajectory": {
    "required_behaviors": ["inspect_before_edit", "verify_after_edit"],
    "forbidden_tools": []
  }
}
