{
  "id": "delegate_subtask_001",
  "category": "task_completion",
  "prompt": "Delegate the banner code update to a worker subagent, then summarize the result.",
  "fixture": "phase_banner",
  "budget": {
    "max_model_calls": 10,
    "max_tool_calls": 10,
    "max_tokens": 35000,
    "max_repair_attempts": 1
  },
  "oracle": {
    "verify_command": "python -m unittest discover -s tests -v",
    "required_changed_files": ["src/banner.py"],
    "allowed_changed_files": ["src/banner.py"],
    "forbidden_changed_paths": [".agent_app", ".git"]
  },
  "trajectory": {
    "required_behaviors": ["verify_after_edit"],
    "forbidden_tools": []
  }
}
