{
  "schema_version": 2,
  "id": "fix_test_failure_001",
  "category": "task_completion",
  "prompt": "Run the unit tests, diagnose the failing math test, fix the implementation, and rerun the tests.",
  "fixture": "simple_math",
  "budget": {
    "max_model_calls": 10,
    "max_tool_calls": 10,
    "max_tokens": 35000,
    "max_repair_attempts": 2
  },
  "oracle": {
    "verify_commands": [
      {"argv": ["python", "-m", "unittest", "discover", "-s", "tests", "-v"], "timeout_seconds": 120}
    ],
    "required_changed_files": ["src/math_utils.py"],
    "allowed_changed_files": ["src/math_utils.py"],
    "forbidden_changed_paths": [".agent_app", ".git"]
  },
  "trajectory": {
    "required_behaviors": ["inspect_before_edit", "verify_after_edit"],
    "forbidden_tools": []
  }
}
