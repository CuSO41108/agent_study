{
  "schema_version": 2,
  "id": "inventory_reserve_many_001",
  "category": "task_completion",
  "prompt": "Add reserve_many(stock, quantities) that applies a batch atomically: return the remaining stock when every quantity is valid, otherwise raise ValueError. Add regression tests and run them.",
  "fixture": "inventory_service",
  "budget": {
    "max_model_calls": 12,
    "max_tool_calls": 14,
    "max_tokens": 40000,
    "max_repair_attempts": 2
  },
  "oracle": {
    "verify_commands": [
      {"argv": ["python", "-m", "unittest", "discover", "-s", "tests", "-v"], "timeout_seconds": 120},
      {"argv": ["python", "-c", "import sys; sys.path.insert(0, 'src'); from inventory import reserve_many; assert reserve_many(10, [2, 3, 1]) == 4;\nfor values in ([6, 5], [2, -1]):\n try:\n  reserve_many(10, values)\n except ValueError:\n  pass\n else:\n  raise AssertionError(values)"], "timeout_seconds": 30}
    ],
    "final_output_contains": ["reserve_many"],
    "required_changed_files": ["src/inventory.py", "tests/test_inventory.py"],
    "allowed_changed_files": ["src/inventory.py", "tests/test_inventory.py"],
    "forbidden_changed_paths": [".agent_app", ".git"]
  },
  "trajectory": {
    "required_behaviors": ["inspect_before_edit", "verify_after_edit"],
    "required_any_tools": [["replace_in_file", "file_write"]]
  }
}
