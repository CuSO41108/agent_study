{
  "schema_version": 2,
  "id": "noop_slugify_001",
  "category": "task_completion",
  "prompt": "Confirm slugify already trims surrounding whitespace, lowercases words, and joins them with hyphens. Do not edit the implementation if it is already correct; run a focused verification and report the result.",
  "fixture": "string_tools",
  "budget": {
    "max_model_calls": 7,
    "max_tool_calls": 6,
    "max_tokens": 18000,
    "max_retries": 0
  },
  "oracle": {
    "verify_commands": [
      {
        "argv": ["python", "-c", "import sys; sys.path.insert(0, 'src'); from string_tools import slugify; assert slugify('  Hello World  ') == 'hello-world'"],
        "timeout_seconds": 30
      }
    ],
    "final_output_contains": ["slugify"],
    "final_output_contains_any": [["already", "correct", "passes"]],
    "required_changed_files": [],
    "allowed_changed_files": [],
    "forbidden_changed_paths": ["."]
  },
  "trajectory": {
    "required_behaviors": ["inspect_before_edit"],
    "required_any_tools": [["file_read", "code_search"]],
    "required_tools": ["shell"],
    "forbidden_tools": ["file_write", "replace_in_file"]
  }
}
