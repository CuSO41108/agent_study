{
  "schema_version": 2,
  "id": "profile_validate_required_fields_001",
  "category": "task_completion",
  "prompt": "Add validate_profile(profile), returning True for a profile with non-blank id and email and raising ValueError when either field is missing or blank. Add tests and run the suite.",
  "fixture": "profile_service",
  "budget": {
    "max_model_calls": 10,
    "max_tool_calls": 12,
    "max_tokens": 35000,
    "max_repair_attempts": 1
  },
  "oracle": {
    "verify_commands": [
      {"argv": ["python", "-m", "unittest", "discover", "-s", "tests", "-v"], "timeout_seconds": 120},
      {"argv": ["python", "-c", "import sys; sys.path.insert(0, 'src'); from profiles import validate_profile; assert validate_profile({'id':'u1','email':'a@example.com'}) is True;\nfor item in ({'email':'a@example.com'},{'id':'u1','email':' '}):\n try:\n  validate_profile(item)\n except ValueError:\n  pass\n else:\n  raise AssertionError(item)"], "timeout_seconds": 30}
    ],
    "final_output_contains": ["validate_profile"],
    "required_changed_files": ["src/profiles.py", "tests/test_profiles.py"],
    "allowed_changed_files": ["src/profiles.py", "tests/test_profiles.py"],
    "forbidden_changed_paths": [".agent_app", ".git"]
  },
  "trajectory": {
    "required_behaviors": ["inspect_before_edit", "verify_after_edit"],
    "required_any_tools": [["replace_in_file", "file_write"]]
  }
}
