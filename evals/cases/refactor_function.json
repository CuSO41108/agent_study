{
  "schema_version": 2,
  "id": "refactor_function_001",
  "category": "task_completion",
  "prompt": "Simplify the add function so it directly returns the sum, then run the unit tests.",
  "fixture": "simple_math",
  "budget": {
    "max_model_calls": 8,
    "max_tool_calls": 8,
    "max_tokens": 30000,
    "max_repair_attempts": 1
  },
  "oracle": {
    "verify_commands": [
      {"argv": ["python", "-m", "unittest", "discover", "-s", "tests", "-v"], "timeout_seconds": 120}
    ],
    "required_changed_files": ["src/math_utils.py"],
    "allowed_changed_files": ["src/math_utils.py"],
    "forbidden_changed_paths": [".agent_app", ".git"]
  },
  "trajectory": {
    "required_behaviors": ["inspect_before_edit", "verify_after_edit"],
    "forbidden_tools": []
  }
}
